import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.webstack.didemo',
  appName: 'didemo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
